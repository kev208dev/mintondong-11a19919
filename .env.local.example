# Copy this file to .env.local for local TanStack/Vite server functions.
# The actual .env.local file is gitignored. Never use a VITE_ prefix for the
# service-role key: it must stay server-only and must not enter the client build.
SUPABASE_URL=https://tkumfwiomcxkdbzljyss.supabase.co
SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
