# 토스페이먼츠 테스트 결제 설정 안내

이 앱의 레슨 **카드/간편결제**는 토스페이먼츠 **SDK V2 + 서버 승인(confirm)** 방식으로 연동돼 있습니다.
현재는 **테스트 모드 전용**입니다. `live_` 키는 앱이 거부하고 "라이브 결제 비활성화"를 표시합니다.

## 1. 테스트 키 발급
1. [토스페이먼츠 개발자센터](https://developers.tosspayments.com/) 로그인
2. **내 개발정보 → 테스트 키**에서 아래 두 값을 복사 (반드시 **같은 상점의 쌍**이어야 합니다)
   - 클라이언트 키: `test_ck_...`
   - 시크릿 키: `test_sk_...`

## 2. Lovable에 환경변수 등록
Project Settings → Secrets 에서 아래 3개를 등록합니다.

| 이름 | 값 | 노출 |
| --- | --- | --- |
| `VITE_TOSS_CLIENT_KEY` | `test_ck_...` | 브라우저 노출 허용 |
| `TOSS_SECRET_KEY` | `test_sk_...` | **서버 전용 (절대 노출 금지)** |
| `ORDER_SIGNING_SECRET` | 랜덤 32자 이상 | 서버 전용 (이미 생성되어 있음) |

키가 없으면 결제 버튼이 비활성화되고 설정 안내 문구가 표시됩니다. **가짜 성공 처리는 하지 않습니다.**

## 3. 테스트 결제 방법
1. 앱에서 `레슨` 탭 → `레슨 신청하기`
2. 코치 → 달력에서 날짜/시간 선택
3. 결제수단에서 **토스 카드/간편결제 (테스트)** 선택 → 결제하기
4. 토스 결제창에서 테스트 카드로 결제 진행
   (토스 테스트 환경은 실제 승인이 발생하지 않습니다.)
5. `/payments/toss/success` 로 리다이렉트 → 서버가 주문/금액을 검증한 뒤
   `POST https://api.tosspayments.com/v1/payments/confirm` 승인
6. 승인되면 예약이 `결제 완료`로 바뀌고 `paymentKey`가 저장됩니다.
7. `예약 취소`를 누르면 서버가 토스 결제취소 API를 호출하고, 성공 시에만 `환불 완료`로 전이됩니다.

## 4. 동작 요약
- 결제창을 열기 **전에** 대기(PENDING) 예약을 만들어 슬롯을 선점 → 중복 예약 방지
- 서버가 `orderId / bookingId / 금액 / 만료`를 HMAC 서명한 주문 토큰 발급
- successUrl 의 `amount` 는 **단독으로 신뢰하지 않고** 서명된 주문과 대조
- 실패/취소 시 슬롯은 다시 열리고, 실패 결제 기록은 삭제하지 않고 보존
- 카드 자격정보는 앱에 **절대** 들어오지 않음 (토스 결제창 내부에서만 처리)

## 5. 프로덕션으로 갈 때 (지금은 하지 않음)
실서비스 결제에는 **가맹점 계약 및 심사**가 필요합니다. 또한 다음이 반드시 필요합니다.
- 주문/예약/결제를 **서버 DB**에 저장하고 DB를 유일한 진실의 원천으로 사용
  (현재 프로토타입은 localStorage + 서명 토큰으로 대체)
- 결제 상태 동기화를 위한 **webhook** 수신 및 서버 측 재검증
- 취소/환불, 정산, 중복 승인 방지에 대한 서버 감사 로그
- 시크릿 키는 서버 환경변수로만 관리 (클라이언트/로그 노출 금지)
